apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "supergraph-operator.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "supergraph-operator.controller.labels" . | nindent 4 }}
