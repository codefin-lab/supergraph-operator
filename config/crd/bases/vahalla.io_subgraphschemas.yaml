---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: subgraphschemas.vahalla.io
spec:
  group: vahalla.io
  names:
    kind: SubgraphSchema
    listKind: SubgraphSchemaList
    plural: subgraphschemas
    singular: subgraphschema
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.routingUrl
      name: URL
      type: string
    - jsonPath: .status.compositionStatus
      name: Status
      type: string
    - jsonPath: .status.lastComposed
      name: Last Composed
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SubgraphSchema is the Schema for the subgraphschemas API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SubgraphSchemaSpec defines the desired state of a federation
              subgraph.
            properties:
              routingUrl:
                description: RoutingUrl is the URL where the subgraph can be reached
                  by the router.
                type: string
              schema:
                description: Schema is the full GraphQL SDL for this subgraph.
                type: string
            required:
            - routingUrl
            - schema
            type: object
          status:
            description: SubgraphSchemaStatus defines the observed state after composition.
            properties:
              compositionStatus:
                description: CompositionStatus indicates whether the last composition
                  succeeded.
                enum:
                - Success
                - Failed
                - Pending
                type: string
              lastComposed:
                description: LastComposed is the timestamp of the last successful
                  composition.
                format: date-time
                type: string
              message:
                description: Message provides human-readable details about the composition
                  result.
                type: string
              supergraphChecksum:
                description: SupergraphChecksum is the SHA-256 of the last composed
                  supergraph.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
